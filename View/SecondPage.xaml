<Page
    x:Class="AppLabo3.View.SecondPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppLabo3.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=MainSecond}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" Margin="40">
            <StackPanel Orientation="Horizontal" Height="Auto">
                <TextBlock Text="Nom:" FontSize="20"/>
                <TextBlock Text="{Binding SelectedStudent.Name}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Height="Auto">
                <TextBlock Text="Age:" FontSize="20"/>
                <TextBlock Text="{Binding SelectedStudent.Age}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
